# Uncomment the next line to define a global platform for your project
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'

target 'TelDir-redux' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TelDir-redux
  pod 'SnapKit', '~> 5.6.0'
  pod 'ReSwift', '~> 6.1.1'

  target 'TelDir-reduxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TelDir-reduxUITests' do
    # Pods for testing
  end

end
